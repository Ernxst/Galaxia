post:
  summary: Sign into an user account
  security: []
  operationId: loginUser
  tags:
    - user_auth
  requestBody:
    required: true
    content:
      application/json:
        schema:
          $ref: "../../openapi.yaml#/components/schemas/UserLoginRequestBody"
  responses:
    $ref: "../../responses/user/login.yaml"
